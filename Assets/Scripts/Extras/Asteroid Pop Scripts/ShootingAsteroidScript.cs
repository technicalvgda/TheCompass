using UnityEngine;
using System.Collections;

public class ShootingAsteroidScript : MonoBehaviour {
	public 
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if(Input.GetMouseButtonDown(1))
			Debug.Log("ASDF");
	
	}
}
